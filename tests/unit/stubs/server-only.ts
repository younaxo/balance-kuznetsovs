// Заглушка пакета `server-only` для тестовой среды: сам пакет
// намеренно бросает исключение вне бандлера Next.js (в браузере/на
// клиенте), но Vitest выполняет код в обычном Node.js, а не через
// сборщик Next — поэтому здесь он безопасно ничего не делает.
export {};
