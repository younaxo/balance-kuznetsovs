/**
 * Минимальный тип услуги для передачи в клиентские компоненты (форма
 * заявки, квиз). Нарочно вынесен в отдельный файл без "server-only" —
 * это просто тип, безопасный для импорта в клиентский код.
 */
export interface ServiceOption {
  slug: string;
  title: string;
}
